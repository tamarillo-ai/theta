# Codex project configuration — AI Coding Tools Orchestrator

model = "o4-mini"
model_reasoning_effort = "medium"

[project]
sandbox_mode = "workspace-write"

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800

[features]
codex_hooks = true
