# Titan Swarm Start v0

Enter Titan swarm-ledger mode.

Use custom agent names, not generic role names:
- `Atlas`, not `architect`
- `Sentinel`, not `reviewer`
- `Mneme`, not `memory-keeper`
- `Forge`, not `coder`
- `Delta`, not `evaluator`

Create or update a Titan swarm ledger before assigning work.

Default:
- Atlas: route frame
- Mneme: lineage/provenance frame
- Sentinel: only after local diff exists and task is narrow
- Delta: only after evidence bundle exists
- Forge: only after mutation gate

No hidden autospawn. No generic role shadowing.
