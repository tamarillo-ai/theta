# Titan Replay: Visible Session v0

Use only visible session export evidence.

Allowed: visible dialogue, executed commands, captured outputs, visible tool/patch calls, compaction markers as boundary evidence.
Forbidden: hidden reasoning, encrypted payload inference, compacted replacement history inference.

Ask Atlas, Sentinel and Mneme to produce candidate packets. Forge and Delta remain locked unless gates are explicit.
