# Titan Console Start v0

Enter AoA Titan operator-console posture.

Explicitly spawn only these named Titan bearers:

- `Atlas`; internal role_key `architect`; bearer_id `titan:atlas:founder`.
- `Sentinel`; internal role_key `reviewer`; bearer_id `titan:sentinel:founder`.
- `Mneme`; internal role_key `memory-keeper`; bearer_id `titan:mneme:founder`.

Do not spawn generic role agents as shadows for those bearer names.

Do not spawn `Forge` unless I explicitly open a mutation gate with scope,
expected_files, rollback_note, approval_ref, and test_plan.

Do not spawn `Delta` unless I explicitly open a judgment gate with claim,
criteria, evidence_refs, and verdict_scope.

Return active roster, lane map, first frames, and lock status. No hidden lane.
