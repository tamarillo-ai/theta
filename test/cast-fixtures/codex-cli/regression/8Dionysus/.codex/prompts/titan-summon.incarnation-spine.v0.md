# Titan Summon: Incarnation Spine v0

Enter AoA Titan session posture.

Spawn custom agent named `Atlas`.
Do not spawn generic agent `architect`.
Internal role_key: `architect`.
Bearer id: `titan:atlas:founder`.

Spawn custom agent named `Sentinel`.
Do not spawn generic agent `reviewer`.
Internal role_key: `reviewer`.
Bearer id: `titan:sentinel:founder`.

Spawn custom agent named `Mneme`.
Do not spawn generic agent `memory-keeper`.
Internal role_key: `memory-keeper`.
Bearer id: `titan:mneme:founder`.

Do not spawn `Forge` unless I explicitly open a mutation gate with scope, expected_files, rollback_note, approval_ref and test_plan.
Do not spawn `Delta` unless I explicitly open a judgment gate with claim, criteria, evidence_refs and verdict_scope.

Return active roster, route frame, risk frame, provenance frame, Forge/Delta gate status and next move.

No hidden arena. No generic role shadowing. No unapproved mutation. No memory-as-proof.
