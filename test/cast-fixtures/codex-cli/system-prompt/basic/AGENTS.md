# Project Guidelines

These instructions apply to every Codex session in this repository.

- Use 2-space indentation in TypeScript.
- Run `npm test` before committing.
- API endpoints live under `src/api/handlers/`.

## Architecture

The service is split into three layers: API, domain, persistence.
